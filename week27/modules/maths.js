function add(a, b) {
    return a + b
}
function sub(a, b) {
    return a - b
}

function mul(a, b) {
    return a * b
}

function div(a, b) {
    return a / b
}
const name = "king of the abyss"

module.exports = {
    addition: add,
    sub,
    mul,
    div,
    name
} 